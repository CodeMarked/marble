// Generated by CMake from MARBLE_SHADER_GLSL_MODULES — do not edit.
// Order matches that list index-for-index. Current sample contract: [0] vertex SPIR-V, [1] lit
// fragment SPIR-V, [2] emissive fragment SPIR-V (see ADR-0057).
#pragma once

#include <cstddef>
#include <string_view>

namespace marble::game_shared {

/// Same basename as `MARBLE_SAMPLE_MESH_SHADERS_PACK_BASENAME` in `game/CMakeLists.txt` (POST_BUILD `.marbpak`).
inline constexpr std::string_view kMarbleSampleMeshShadersPackFileName = "@MARBLE_SAMPLE_MESH_SHADERS_PACK_BASENAME@";

inline constexpr std::string_view kMarbleSampleShaderRegistryPaths[] = {
@MARBLE_SAMPLE_SHADER_REGISTRY_PATHS_BODY@
};

inline constexpr std::string_view kMarbleSampleShaderSpvBasenames[] = {
@MARBLE_SAMPLE_SHADER_BASENAME_PATHS_BODY@
};

inline constexpr std::size_t kMarbleSampleShaderCount =
    sizeof(kMarbleSampleShaderRegistryPaths) / sizeof(kMarbleSampleShaderRegistryPaths[0]);

static_assert(
    sizeof(kMarbleSampleShaderRegistryPaths) / sizeof(kMarbleSampleShaderRegistryPaths[0]) ==
        sizeof(kMarbleSampleShaderSpvBasenames) / sizeof(kMarbleSampleShaderSpvBasenames[0]),
    "MarbleSampleShaderNames: registry path count must match SPIR-V basename count");

static_assert(kMarbleSampleShaderCount == 3, "MarbleSampleShaderNames: sample mesh init expects exactly three modules");

} // namespace marble::game_shared
